@AGENTS.md

# Claude Code adapter

Use `.claude/agents/reviewer.md` for independent review. Project skills are
generated under `.claude/skills/`; run `python scripts/sync_adapters.py --check`
before completion. See `adapters/claude/README.md` for worktree and hook usage.
